const initialState = {
  login: "",
  password: "",
};
export const loginReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export const login = () => (dispatch) => {};
